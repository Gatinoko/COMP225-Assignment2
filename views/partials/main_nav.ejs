<!-- Main navigation bar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">

  <!-- Navigation bar logo -->
  <a class="navbar-brand" href="/"><img src="./Assets/images/GlogoTransparent.png" height="50px"></a>

  <!-- Dropdown menu for the compact navigation bar -->
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <!-- Navigation bar list -->
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">

      <!-- Logic for the active items selection -->
      <!-- Home navigation item-->
      <% if (title == "Home"){ %>
      <li class="nav-item active">
      <% } else  { %>
      <li class="nav-item">
      <% } %>
        <a class="nav-link" href="/home"><i class="fas fa-home"></i></i> Home </a> 
      </li>

      <!-- About navigation item-->
      <% if (title == "About") { %>
      <li class="nav-item active">
      <% } else  { %>
      <li class="nav-item">
      <% } %>
        <a class="nav-link" href="/about"><i class="fas fa-info-circle"></i> About </a>
      </li>

      <!-- Projects navigation item-->
      <% if (title == "Projects") { %>
      <li class="nav-item active">
      <% } else  { %>
      <li class="nav-item">
      <% } %>
        <a class="nav-link" href="/projects"><i class="fas fa-tasks"></i> Projects </a>
      </li>

      <!-- Services navigation item-->
      <% if (title == "Services") { %>
      <li class="nav-item active">
      <% } else  { %>
      <li class="nav-item">
      <% } %>
        <a class="nav-link" href="/services"><i class="fas fa-cogs"></i> Services </a>
      </li>

      <!-- Contact navigation item-->
      <% if (title == "Contact") { %>
      <li class="nav-item active">
      <% } else  { %>
      <li class="nav-item">
      <% } %>
        <a class="nav-link" href="/contact"><i class="fas fa-mobile-alt"></i> Contact </a>
      </li>
  </div>
</nav>